.row
  .col-lg-12
    .ibox.float-e-margins
      .ibox-content
        .row
          .col-sm-3
            -if can? :create, Pais
              = link_to "Nuevo Pais", { controller: "paises", action: "new" }, class: "btn btn-primary btn-block"
        %hr
        %br
        .row
          .col-md-12
            %table.table.table-striped.table-bordered.table-hover.dataTables-example
              %thead.blue-bg
                %tr
                  %th Nombre
                  %th Idioma
                  %th Acciones
              %tbody
                - @paises.all.each do |pais|
                  %tr
                    %td= pais.nombre
                    %td= pais.idioma.nombre
                    %td
                      -if can? :update, Pais
                        = link_to edit_pais_path(pais) do
                          %i.fa.fa-pencil-square-o
                          Editar
                      &nbsp;
                      &nbsp;
                      &nbsp;
                      -if can? :destroy, Pais
                        = link_to pais, data: { confirm: "Seguro que desea eliminar el Pais ?" }, method: :delete do
                          %i.fa.fa-trash
                          Eliminar